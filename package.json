{
  "name": "fndrs-lead-engine-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel --filter ./cliente --filter ./server dev",
    "dev:cliente": "pnpm --filter ./cliente dev",
    "dev:server": "pnpm --filter ./server dev",
    "build": "pnpm -r --filter ./cliente --filter ./server build"
  }
}
